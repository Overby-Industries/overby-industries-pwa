// next.config.ts
const nextConfig = {
  reactStrictMode: true,

  // Allows importing SVGs as static images via next/image
  images: {
    disableStaticImages: true,
  },
};

export default nextConfig;